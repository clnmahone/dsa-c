#include <stdio.h>
#include<stdlib.h>
struct A{
	int a;
} A;

int main(){


    int i=0;
    struct A* a = (struct A*)malloc(sizeof(struct A));
    while (i++<9999)
    {
        if(a);
    }
    


    return 0;
}
